#id ela vai buscar as variaveis de ambiente 
#Gn vai pegar os grupos pelos nomes (n)
#tr vai fazer a formatação das linhas 
#-d é um subcomando do tr que deleta a outra linha (\n)
id -Gn $FT_USER | tr ' ' ',' | tr -d '\n'
 
